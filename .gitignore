node_modules/
dist/
coverage/
*.tsbuildinfo

# Local DayCrew state and secrets
# Workspace runtime state lives in a .daycrew/ directory inside the selected
# Workspace root. It is never source, so ignore it at any depth in this repo.
.daycrew/
.env
.env.*
!.env.example

# Editors and operating systems
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
