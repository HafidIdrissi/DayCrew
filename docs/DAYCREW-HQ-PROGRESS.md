# DayCrew HQ — journal de progression

## 2026-09-15 — P0 audité, P1 implémentée

- **État initial :** branche `mvp` alignée sur `origin/mvp`, sans changements non validés au début de la tâche. Instructions du dépôt, code, tests et brief complet inspectés. P0 décrit dans [DAYCREW-HQ-SPEC.md](./DAYCREW-HQ-SPEC.md).
- **Référence avant modification :** `pnpm check` passait, 32 fichiers et 249 tests.
- **Livré en P1 :** navigation HQ / Needs You / Tasks / Meetings / Library avec Teams contextuelles ; HQ Living/Focus alimenté par `/api/home` ; studios, missions et résultats enregistrés ; discussions et sections Tasks, Deliverables, Members ; éditeur d'agent avec identité, instructions, moteur, modèle et effort compatible ; validation serveur, persistance et transmission par agent à Claude Code et Codex CLI. Auto, anciens fichiers `.daycrew/`, routes utiles, limites et protections du Workspace demeurent en place.
- **Vérifié automatiquement :** `pnpm check` complet réussi (lint, build, typecheck, 32 fichiers, 254 tests). Les nouveaux tests couvrent la compatibilité exacte d'un effort, les arguments de lancement par agent, la persistance après redémarrage et le refus d'une configuration incompatible. `git diff --check` ne signale aucune erreur de diff.
- **Inspecté dans le navigateur :** application locale ouverte sur le Workspace `test`. HQ Living et Focus, discussion, éditeur d'agent, sections de Team et aperçu Réunions inspectés. Le badge global « Needs You » reflète aussi la décision d'une autre Team depuis la vue Members. Trois captures réelles de bureau : [HQ](./hq-captures/hq-desktop.jpg), [discussion](./hq-captures/team-desktop.jpg), [éditeur](./hq-captures/agent-desktop.jpg). La capture HQ précède une correction mineure de grammaire ; le texte final a été confirmé dans l'arbre accessible.
- **Contrôles responsive possibles :** largeur CSS 1440 sur Réunions, 1024 sur Réunions, 768 sur HQ, 390 sur HQ, discussion, éditeur et Réunions. À 390, le tiroir Teams s'ouvre et les blocs principaux mesurés tiennent dans le viewport ; l'éditeur et Réunions ne produisent pas de débordement horizontal global. Le navigateur a parfois réappliqué son viewport par défaut entre deux actions : les mesures ont été prises immédiatement après chaque réglage.
- **Non vérifié :** aucun tour avec un vrai compte moteur. Le catalogue local `codex debug models` (Codex CLI 0.154.0) a été consulté, sans génération facturée. Les tests d'adaptateur utilisent de faux CLI ; ils prouvent la transmission des arguments, pas leur acceptation par un compte réel. L'outil de capture navigateur a expiré à répétition pour Réunions et les petites largeurs ; aucune capture visuelle de ces écrans n'est annoncée. La phase 2 (réunions réelles) et la phase 3 (nouveaux adaptateurs, notamment Gemini CLI) ne sont pas implémentées.

**Point de reprise :** partir de P1 sur `mvp`, relire les décisions dans `DAYCREW-HQ-SPEC.md` et le brief. En P2, ajouter une orchestration réelle des réunions avec contexte partagé, décisions durables et garde-fous de Workspace avant d'activer le bouton. En P3, créer les adaptateurs explicitement prévus et leurs catalogues vérifiés sans réutiliser l'identité `gemini` d'Antigravity. Une validation manuelle avec un compte Claude/Codex réel reste utile avant de considérer le choix d'effort comme éprouvé en production.
